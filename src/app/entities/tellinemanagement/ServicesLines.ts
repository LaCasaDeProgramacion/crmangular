import { User } from 'src/app/entities/user';
export interface ServicesLines {
     id ?: number;
	 serviceDescription ?: string;
	 serviceName ?:string;
	 activationCode ?: string;
	 enabled ?: boolean;
	 
}